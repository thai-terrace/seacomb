# An aarch64 guest, which covers Arch::Aarch64.

base:
- common.yaml
- template:_images/debian-13

arch: aarch64

# An aarch64 kernel reports Arch::Arm for a 32-bit process, but Apple Silicon
# cannot execute AArch32 at all, so Arch::Arm gets its own guest.
